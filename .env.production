# Production Environment Configuration
# Used for cloud deployment (Vercel/Netlify)

# Cloud Backend URL (Render hosted)
VITE_BACKEND_URL=https://timesyncprobackend.onrender.com

# Local Fingerprint Service (always localhost for kiosk devices)
VITE_FINGERPRINT_SERVICE_URL=http://localhost:8080

# Bridge URL for ZKTeco network terminals (optional)
VITE_BRIDGE_URL=http://localhost:3000
